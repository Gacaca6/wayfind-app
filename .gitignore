# Dependencies
node_modules/

# Build output
dist/
dist-ssr/

# Environment variables — DO NOT commit API keys
.env
.env.local
.env.*.local

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor / OS
.vscode/
.DS_Store
Thumbs.db

# Cache
.vite/

# Copyrighted Kinyarwanda Bible text — bundled locally so installed apps work
# offline, but NOT committed to this public repo (© Bible Society of Rwanda).
# Regenerate locally with: npm run data:kin
public/bible/kin.json

# Hymnal text (Gushimisha + Agakiza) — church hymnbook compilations without an
# explicit open license; same treatment as kin.json. Regenerate: npm run data:hymns
public/hymns/hymns.json
